/**
 * WhisperBridge.ts
 * * TypeScript interface for the C++ WhisperEngine.
 * Handles the async communication with the native STT engine.
 */
export interface WhisperConfig {
    modelPath: string;
    language: string;
}
// Implementation follows...
