/**
 * MeetingController.ts
 * * Orchestrates the Asynchronous Audio Buffer Strategy.
 * - Controls Whisper (STT) stream
 * - Triggers Llama (Qwen) every 5 minutes
 * - Manages the raw audio buffer during Llama inference
 */
